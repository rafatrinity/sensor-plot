{
  "name": "sensor-plot",
  "version": "1.0.0",
  "description": "objective: subscribe on mqtt broker, and plot chart.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "lerna run build --parallel",
    "start": "lerna run start --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "Rafael Trindade",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
